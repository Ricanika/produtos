
# STLs intermediarios das folhas de validacao: sao regeraveis rodando o
# proprio script (fechado.py, listras.py, rasgos.py, frente.py) e pesam ~5 MB
# cada.
# So cesto-aba.stl -- a peca -- e versionado.
fechado-*.stl
lis-*.stl
rasgo-*.stl
frente-*.stl
col-*.stl
ext-*.stl
af-*.stl
vr-*.stl

# STLs do visor da linha (regeraveis por visor_elo.py)
elo-p.stl
elo-m.stl
elo-g.stl
ver-m.stl

# elo-medidas.json NAO entra aqui de proposito: e o unico lugar onde as
# medidas do M e do G vivem, custam ~40 min de booleano para refazer, e sao o
# que permite redesenhar elo.png sem reconstruir os solidos.
